# Backup from Git Pull Recovery (2026-02-23)
This directory contains untracked files that were moved to resolve conflicts during a 'git pull'.
The repository now contains the upstream versions of these files.

Compare these versions with the repository versions to restore any local customizations.
Specifically:
- config.toml (Encoder/CAM settings)
- shutter-deck-fam.service (Path/Environment settings)
- shutter_deck/ and tests/ (Feature/v2 persistence changes)
